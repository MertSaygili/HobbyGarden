package hobby_garden.hobby_garden_server.common.enums;
//* Log level enum, used to determine the log level of the log,
//* Application level, used to determine the log level of the log
public enum LogLevel {
    INFO,
    WARNING,
    ERROR,
    FATAL,
}
